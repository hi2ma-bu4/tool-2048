{
  "name": "2048-best-move-calculator",
  "version": "1.0.0",
  "description": "A tool to calculate the best move in the game 2048.",
  "main": "dist/main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --platform=browser && esbuild src/ai/ai-worker.ts --bundle --outfile=dist/ai-worker.js --platform=browser",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "esbuild": "^0.19.5",
    "typescript": "^5.2.2"
  }
}
